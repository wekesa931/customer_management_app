/* All items on the Modal module are rendered here */
import React from 'react';
import ModalTab from './CustomerDetails/Modal/modal_file';

const Home = () => {
    return (
        <div>
            <ModalTab/>
        </div>
    );
};

export default Home;